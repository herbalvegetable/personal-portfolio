import { useState, useEffect, useRef } from 'react';

import styles from './Collage.module.css';

export default function Collage({ children }){

    useEffect(() => {

    }, []);

    return (
        <div className={styles.collage}>

        </div>
    )
}